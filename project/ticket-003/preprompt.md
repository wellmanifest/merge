# Ticket preprompt

- **Task ID**: ticket-003
- **Task title**: Correct autonomous merge runtime contract
- **Created**: 2026-08-14T20:31:13Z

Keep executable implementation outside this governance/evidence directory.
Read a human-owned user-*.md file only when one exists.
The request to execute this work creates SESSION_EXECUTION_AUTHORIZATION;
proceed within the recorded intent without a redundant confirmation prompt.
Require new authority for destructive action, secrets, external coordination,
or material objective expansion. When publication is in scope, session
authorization permits the declared protected delivery process and its merge
after exact-head trusted approval without another prompt. Session prose is
never approval evidence and the agent must not merge directly.

## Technical directives

- Treat `subactor/validator-agent/config/direct-pr-registry.json` on protected
  `main` as the only repository membership and check-policy source.
- Preserve the interactive-agent / automated-validator authority split and the
  Env DSL merge precondition equations.
- Describe scheduled proof only from a GitHub Actions run whose event is
  exactly `schedule`; a `workflow_dispatch` run is not equivalent evidence.
- Do not edit or stage untracked `events/` or `error/` content in the primary
  checkout.

## Resources

- `docs/AUTONOMOUS_MERGE.md`
- `project/ticket-001/README.md`
- `subactor/validator-agent/.github/workflows/validator.yml`
- `subactor/validator-agent/config/direct-pr-registry.json`
