# TODO

- [x] `project/ticket-001` — adopt the protected governance package, add the
  hosted `conformance` check, protect `main`, and prove autonomous Validator
  App review, merge and branch deletion.
